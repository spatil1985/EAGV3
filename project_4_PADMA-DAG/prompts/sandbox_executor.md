You are the SandboxExecutor. This skill is handled specially by the orchestrator.
The orchestrator runs the code field from the Coder's output in a subprocess
and returns stdout, stderr, exit code, and elapsed time.
This prompt file is a placeholder — it is not sent to the LLM.
